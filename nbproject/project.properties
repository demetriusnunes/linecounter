application.title=LineCounter
application.vendor=dema
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
build.generated.sources.dir=${build.dir}/generated-sources
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/LineCounter.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
file.reference.foxtrot.jar=lib/foxtrot.jar
file.reference.jruby-complete.jar=../monkeybars/dist_/lib/jruby-complete.jar
file.reference.swing-layout-1.0.2.jar=../monkeybars/dist_/lib/swing-layout-1.0.2.jar
includes=**
jar.compress=false
javac.classpath=\
    ${file.reference.jruby-complete.jar}:\
    ${file.reference.swing-layout-1.0.2.jar}:\
    ${libs.swing-layout.classpath}:\
    ${libs.swing-app-framework.classpath}:\
    ${libs.absolutelayout.classpath}:\
    ${file.reference.foxtrot.jar}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
# Property libs.absolutelayout.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.absolutelayout.classpath=../../../../Applications/NetBeans/NetBeans 6.0.app/Contents/Resources/NetBeans/java1/modules/ext/AbsoluteLayout.jar
# Property libs.swing-app-framework.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.swing-app-framework.classpath=../../../../Applications/NetBeans/NetBeans 6.0 Beta 2.app/Contents/Resources/NetBeans/java1/modules/ext/appframework-1.0.2.jar:../../../../Applications/NetBeans/NetBeans 6.0 Beta 2.app/Contents/Resources/NetBeans/java1/modules/ext/swing-worker-1.1.jar
# Property libs.swing-layout.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.swing-layout.classpath=../../../../Applications/NetBeans/NetBeans 6.0 Beta 2.app/Contents/Resources/NetBeans/platform7/modules/ext/swing-layout-1.0.3.jar
main.class=linecounter.Main
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
source.encoding=UTF-8
src.dir=src
test.spec.dir=spec
test.src.dir=test
